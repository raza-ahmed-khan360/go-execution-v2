import Link from "next/link";
import { DesktopHeroBackground } from "@/components/desktop-hero-background";
import { MobileHeroStory } from "@/components/mobile-hero-story";

function Content({ eyebrow, title, copy, showActions = true }: { eyebrow: string; title: string; copy: string; showActions?: boolean }) { return <div className="ge-hero__content"><p className="ge-eyebrow ge-eyebrow--light ge-hero-reveal"><span />{eyebrow}</p><h1 className="ge-hero__title"><span className="ge-hero-reveal">{title}</span></h1><p className="ge-hero__copy ge-hero-reveal">{copy}</p>{showActions ? <div className="ge-hero__actions ge-hero-reveal"><Link className="ge-button ge-button--gold ge-magnetic" href="/contact">Book a Free Consultation</Link><Link className="ge-text-link" href="/portfolio">Explore our work</Link></div> : null}</div>; }
export function PageHero({ eyebrow, title, copy, contact = false }: { eyebrow: string; title: string; copy: string; contact?: boolean }) { return <><section className={`ge-hero ge-hero--desktop ge-hero--inner${contact ? " ge-hero--contact" : ""}`}><DesktopHeroBackground interactive={!contact} /><div className="ge-container ge-hero__centered-container"><Content eyebrow={eyebrow} title={title} copy={copy} showActions={!contact} /></div>{!contact ? <div className="ge-hero__scroll" aria-hidden="true"><span />Scroll to execute</div> : null}</section><MobileHeroStory variant={contact ? "contact" : "inner"} disableMotion={contact} eyebrow={eyebrow} title={title} titleAccent="" copy={copy} primaryLabel="Book a Free Consultation" primaryHref="/contact" secondaryLabel="Explore our work" secondaryHref="/portfolio" /></>}

export function ConsultationCta(){return <section className="ge-inner-cta"><div className="ge-container ge-inner-cta__inner ge-reveal"><div><p className="ge-eyebrow ge-eyebrow--light">Move from idea to impact</p><h2>Let’s turn your next priority into focused execution.</h2></div><Link className="ge-button ge-button--gold ge-magnetic" href="/contact">Book a Free Consultation</Link></div></section>}
