import {ConsultationCta,PageHero} from "@/components/page-hero";
export type LegalData={intro:string[];sections:{title:string;paragraphs:string[]}[]};
export function LegalPage({eyebrow,title,copy,data}:{eyebrow:string;title:string;copy:string;data:LegalData}){return <main id="primary" className="site-main"><PageHero {...{eyebrow,title,copy}}/><section className="ge-section ge-legal" style={{paddingTop:'var(--ge-spacing-lg)',paddingBottom:'var(--ge-spacing-lg)'}}><div className="ge-container"><div className="ge-legal__content" style={{maxWidth:800,margin:'0 auto'}}>{data.intro.map((p,i)=><div className="ge-reveal" style={{marginBottom:'2rem'}} key={i}><p>{p}</p></div>)}{data.sections.map(s=><div className="ge-reveal" style={{marginBottom:'2rem'}} key={s.title}><h2>{s.title}</h2>{s.paragraphs.map((p,i)=><p key={i}>{p}</p>)}</div>)}</div></div></section><ConsultationCta/></main>}
